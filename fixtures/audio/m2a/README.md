# Synthetic M2a candidate audio

Every WAV in this directory is generated from the matching text in
`seed/candidates/{a,b,c}/m2a-session.json` with the open-source eSpeak synthesizer.
They contain no recording or voice of a real person. They exist only to make the
M2a record/replay path deterministic and must never be replaced with human audio.
